# Source Directory

The current implementation is maintained as a single Arduino sketch.

The production-ready source is located in:

`arduino_code/heartbeat_monitor.ino`

The `src/` directory is reserved for future modularization.